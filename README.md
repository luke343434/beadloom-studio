# 串拾 Beadloom Studio

一个可交互的串珠设计模拟器。用户可以从珠子库拖入珠子、调整排列、独立编辑尺寸和颜色、多选批量操作，并导出 PNG 设计图。

## 本地运行

```bash
npm install
npm run dev
```

## 生产构建

```bash
npm run build
```

推送到 `main` 分支后，GitHub Actions 会自动构建并部署到 GitHub Pages。
